# Optional development lab

The retained desktop/Virtual Lab is an optional development and visualization
surface. It may depend on ReleaseGate Core; Core must never depend on it.

No existing lab source was moved in the first migration round because active
desktop and source-string conformance tests still reference its current paths.
