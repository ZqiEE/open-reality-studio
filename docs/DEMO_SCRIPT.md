# Demo Script

This script is for a real software screen recording of the current Public Alpha. Do not replace it with a concept video.

## Core Message

The demo answers one question on real hardware: **what did the AI intend,
was it allowed, and where is the proof?** A plain-English goal is gated,
refused when unsafe, and receipted — you watch one command reach a real
servo, then watch the next one get blocked with an honest, exportable record.
The refusal and its receipt are the product.

RealityWarden is the black box and gatekeeper for AI-driven machines: a
neutral, evidence-grade safety gateway between AI intent and real actuators.
It builds no robot, no chip, and no model.

The same software boundary — reviewed profiles, capability checks, live
evidence, safety decisions, a ticketed hardware gate, and honest audit logs —
is what lets more devices become governable later. That ecosystem (open
protocol, adapters, marketplace) is chapter two; the gate and the receipt
lead.

Current boundary: REAL-device-first, one documented ESP32 reference rig only.
SIM LAB remains a separately selected zero-signal tool and is never a fallback
from a failed REAL operation.

## Target Length

30-45 seconds

## Recording Flow

1. Open RealityWarden Desktop.
2. Show REAL selected, the primary REAL HARDWARE boundary, and the connected
   ESP32 reference rig.
3. Add opening caption:

```text
Intent does not go straight to hardware.
RealityWarden validates, checks live evidence, gates, and audits every command.
```

4. Keep the visible per-session confirmation checked and issue an explicit
   supported angle such as `45°`.
5. Show the safety decision, physical servo response, `hardwareSignalSent:true`,
   and the REAL twin labeled open-loop/not measured.
6. Put a hand inside the configured safe distance (or disconnect the sensor),
   then issue another explicit angle.
7. Show the command blocked with `hardwareSignalSent:false` and zero subsequent
   frames.
8. Briefly select SIM LAB to show that virtual workflows are a separate blue,
   zero-signal mode—not the default product surface.
9. End on this statement:

```text
RealityWarden = the black box and gatekeeper for AI-driven machines: every action gated, refusable, receipted.
REAL-first Public Alpha. One governed reference rig. Simulation stays separate.
```

## Suggested On-Screen Captions

```text
It refuses — and shows you the evidence.
```

```text
A per-action receipt a regulator, insurer, or customer will accept.
```

```text
Physical AI should not be locked inside one brand.
```

```text
Ordinary hardware is not automatically AI-controllable.
```

```text
Intent -> validation -> live evidence -> safety gate -> hardware ticket -> honest audit.
```

```text
More devices. More Reality Assets. More adapters. Less closed monopoly.
```

```text
AI should not touch reality directly.
```

## Notes

- Use the real desktop app UI.
- Do not splice in concept trailers.
- Do not imply general or production hardware execution; if the reference rig
  is mentioned, show its independent REAL HARDWARE boundary and evidence lock.
- Do not claim unsupported device families are runnable.
- Do not claim that purely mechanical objects without electronics or interfaces can be controlled by software alone.
- Use language like "ordinary hardware with chips, controllers, sensors, actuators, motors, or hardware interfaces" when explaining future device scope.
