[
  {
    "schemaVersion": 1,
    "integration": "clearpath-generator",
    "status": "reference-contract",
    "stableApprovedInputs": ["robot.yaml digest", "generator package identity and version"],
    "runtimeCapabilities": [],
    "excludedVolatileInputs": ["regenerated formatting", "timestamps", "output path"],
    "mismatchBehavior": "block-before-dispatch",
    "externalTestGate": "Run against a real Clearpath generated workspace; no official support is claimed before that result."
  },
  {
    "schemaVersion": 1,
    "integration": "ros2-canopen-command-path",
    "status": "reference-contract",
    "stableApprovedInputs": ["stable device-to-role mapping", "EDS/DCF digest", "adapter identity and version"],
    "runtimeCapabilities": ["canopen.command_path.operational"],
    "excludedVolatileInputs": ["CAN Node ID by itself", "unrelated bus participants"],
    "mismatchBehavior": "block-before-dispatch",
    "externalTestGate": "Exercise authenticated topology and NMT observations with ros2_canopen; raw Node IDs cannot pass."
  },
  {
    "schemaVersion": 1,
    "integration": "nav2-velocity-smoother",
    "status": "reference-contract",
    "stableApprovedInputs": ["Nav2 distribution and release identity for version-specific execution semantics", "selected velocity-smoother command-semantics digest covering feedback, smoothing frequency, vector scaling, velocity/acceleration/deceleration limits, deadband, timeout and execution-relevant timestamp behavior", "CLOSED_LOOP-only odometry source/topic, observation duration and selected frame/source identity", "resolved controller-output to velocity-smoother to optional execution-critical gates to base-command-consumer topology", "allowed FollowPath controller, goal-checker and progress-checker implementation identities and selected configuration for ROS 2 Jazzy"],
    "runtimeCapabilities": ["nav2.command_path.ready"],
    "excludedVolatileInputs": ["current odometry or velocity sample", "planner-local observation noise", "unselected ROS graph participants and unrelated parameters"],
    "mismatchBehavior": "block-before-dispatch",
    "externalTestGate": "A future Jazzy FollowPath adapter must bind each exact pre-dispatch controller_id, goal_checker_id and progress_checker_id selection; prove semantic/configuration/source/topology drift and selector substitution deny before dispatch, while volatile odometry sample changes alone do not. Jazzy has no path_handler_id field."
  },
  {
    "schemaVersion": 1,
    "integration": "elite-cs-model",
    "status": "reference-contract",
    "stableApprovedInputs": ["reported robot model", "driver/SDK identity and version"],
    "runtimeCapabilities": ["elite.command_path.ready"],
    "excludedVolatileInputs": ["marketing family name", "network address"],
    "mismatchBehavior": "block-before-dispatch",
    "externalTestGate": "An Elite-owned observer must prove CS63/CS66 mismatch detection before support is claimed."
  },
  {
    "schemaVersion": 1,
    "integration": "crane-x7-selected-limits",
    "status": "reference-contract",
    "stableApprovedInputs": ["selected URDF hardware-level limits digest", "selected MoveIt planning-constraints digest only when approved command semantics depend on planner output", "selected ros2_control hardware-drive limits and configuration digest"],
    "runtimeCapabilities": ["crane_x7.motion_ready"],
    "excludedVolatileInputs": ["live encoder/controller posture (fresh runtime state or selected observed-state continuity, never static approval identity)", "unselected MoveIt planning constraints", "unrelated planning scene and state"],
    "mismatchBehavior": "block-before-dispatch",
    "externalTestGate": "A CRANE-X7 simulator or owner must prove that a selected static source change denies dispatch, an unselected MoveIt change does not globally invalidate, and missing, stale or unknown selected posture fails closed."
  },
  {
    "schemaVersion": 1,
    "integration": "device-serial-calibration",
    "status": "reference-contract",
    "stableApprovedInputs": ["device serial to role mapping", "per-device calibration digest"],
    "runtimeCapabilities": ["device.calibration.loaded"],
    "excludedVolatileInputs": ["USB port number", "enumeration order"],
    "mismatchBehavior": "block-before-dispatch",
    "externalTestGate": "Swap USB ports and then swap physical serials; only the latter must invalidate authorization."
  },
  {
    "schemaVersion": 1,
    "integration": "physical-execution-identity",
    "status": "reference-contract",
    "stableApprovedInputs": ["selected base robot-description digest", "selected actuator and sensor configuration digests", "controller interface and command-semantics digests", "execution-relevant runtime, software and configuration identities"],
    "runtimeCapabilities": [],
    "excludedVolatileInputs": ["simulator world", "visual assets", "incidental simulator plugins", "unselected interfaces and sensors", "unrelated environment observations"],
    "mismatchBehavior": "block-before-dispatch",
    "externalTestGate": "Change one selected interface semantic source and require denial while an unselected interface or simulator-only asset change leaves the approval identity unchanged."
  },
  {
    "schemaVersion": 1,
    "integration": "ros2-control-runtime-compatibility",
    "status": "reference-contract",
    "stableApprovedInputs": ["selected controller and hardware-interface configuration", "ros2_control and hardware_interface execution-semantics identity", "integration-qualified compatibility-envelope identity when one exists"],
    "runtimeCapabilities": [],
    "excludedVolatileInputs": ["unchanged action or topic name by itself", "unrelated ROS packages", "simulator-only world and visual state"],
    "mismatchBehavior": "block-before-dispatch",
    "externalTestGate": "Change lifecycle, resource, timing or failure semantics beneath an unchanged public interface and require denial unless the adapter maps both runtimes to the same reviewed compatibility envelope."
  }
]
