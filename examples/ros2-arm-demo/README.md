# ROS 2 arm demo boundary

This directory documents the intended reference-demo location. The first
migration phase defines and tests only the in-memory gateway contracts in
`packages/ros2-gateway`; it does not claim working or production ROS 2
networking.
