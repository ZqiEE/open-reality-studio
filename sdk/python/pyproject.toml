[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rlsok"
version = "1.3.0"
description = "Policy proposal client for the local RLSOK robot execution gate"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["rlsok"]
